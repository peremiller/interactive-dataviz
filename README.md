# Interactive Data Explorer

A self-contained interactive data visualization webpage (Chart.js, single HTML file).
Filter series, switch chart types, adjust the time range, swap datasets, toggle dark/light, or upload your own CSV — all client-side.
